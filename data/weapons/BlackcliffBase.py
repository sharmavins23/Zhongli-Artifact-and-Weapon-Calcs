# Static data class for blackcliff pole with zero stacks
class BlackcliffPole_Base:
    level = 90
    refinementLevel = 1

    name = "Blackcliff Pole (no stacks)"

    # Base stat values
    baseATK = 510
    critDMG = 0.551

    # Passive values - no stacks, so no passive stats
